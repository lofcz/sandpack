{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sandpack monorepo deploy config",
  "type": "object",
  "required": ["targets"],
  "properties": {
    "targets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "from", "into", "root"],
        "properties": {
          "name": { "type": "string" },
          "from": {
            "type": "string",
            "description": "Build artifact path relative to the monorepo root. The token {binary} is replaced by the platform binary name."
          },
          "into": { "type": "string", "description": "Subdirectory under the resolved root to copy into." },
          "root": {
            "type": "string",
            "description": "Logical root name; resolved from --<root> CLI flag or SANDPACK_DEPLOY_<ROOT> env (uppercased, dashes→underscores)."
          },
          "enabled": { "type": "boolean", "default": true },
          "comment": { "type": "string" }
        }
      }
    }
  }
}
